%{
#include "y.tab.h"
%}

%%
do             { return DO; }
while          { return WHILE; }
"("            { return LP; }
")"            { return RP; }
[0-9]+         { yylval = atoi(yytext); return NUMBER; }
[a-zA-Z]+      { yylval = strdup(yytext); return ID; }
[ \t\n]+       { /* Ignore whitespace */ }
.              { /* Ignore other characters */ }
%% 
yacc -d do_while_syntax.y  # Generates y.tab.c and y.tab.h
flex lexer.l                # Generates lex.yy.c
gcc y.tab.c lex.yy.c -o do_while_syntax -lfl

./do_while_syntax
do 
    ; 
while (x);
